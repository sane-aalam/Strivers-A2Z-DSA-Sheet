
1. create visited array (V,0)
2. traversal the graph + make sure checking the current node is visited or not visited

for(int node = 0; node < V; node++){
    if(visitedArray[node] == 0){
        // Call Traversal Algorithm DFS/BFS
    }
}